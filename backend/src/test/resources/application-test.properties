# Test configuration for both unit and integration tests

# Application name
spring.application.name=cibaria

# H2 Database for integration tests
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# JPA configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Server configuration
server.servlet.context-path=/api

# JWT configuration for tests
SECRET_KEY=dGVzdC1zZWNyZXQta2V5LWZvci1qd3QtdG9rZW4tdGVzdGluZy12ZXJ5LWxvbmctc2VjdXJlLWtleQ==

# Cloudinary configuration (mocked values)
API_KEY=test-api-key
API_SECRET_KEY=test-secret-key
CLOUD_NAME=test-cloud-name

# Logging (reduced for cleaner test output)
logging.level.org.springframework.security=WARN
logging.level.com.kk.cibaria=INFO